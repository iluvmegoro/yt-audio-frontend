services:
  - type: web
    name: yt-audio-node
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: PYTHON_API
        value: https://yt-audio-api-q9x4.onrender.com  # ← FlaskのURL
